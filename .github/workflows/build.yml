name: Build ROM

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-none-eabi binutils-arm-none-eabi libpng-dev

      - name: Fetch and install agbcc
        run: |
          mkdir -p tools/agbcc
          # --strip-components=1 tells tar to ignore the top-level 'agbcc-master' folder
          curl -L https://github.com/pret/agbcc/archive/master.tar.gz | tar -xz -C tools/agbcc --strip-components=1
          
          # Now let's verify where the files actually landed
          ls -R tools/agbcc
          
          # Ensure we are pointing to the correct binary location
          chmod +x tools/agbcc/bin/agbcc
      - name: Build tools
        run: make tools

      - name: Build ROM
        run: make

      - name: Upload ROM artifact
        uses: actions/upload-artifact@v4
        with:
          name: game-rom
          path: |
            *.gba
